# 기획서 (spec.md) — 영상 번역 자막 앱

> 작업 진행에 따라 지속 업데이트. 변경 시 하단 [변경 이력](#변경-이력)에 기록.

## 1. 제품 개요

모바일에서 앱 내 영상(로컬 파일/URL)을 재생할 때, **영상 속 음성의 언어를 자동 인식**하고
**번역**하여 재생과 동기화된 **자막을 자동으로 표시**하는 Flutter 크로스플랫폼 앱.

- **대상 사용자**: 외국어 영상(강연·뉴스·브이로그 등)을 모국어 자막으로 보고 싶은 사용자.
- **핵심 가치**: 자막이 없는 영상에도 즉시 번역 자막을 입혀 이해도를 높인다.

## 2. 확정된 방향(결정 사항)

| 항목 | 결정 | 비고 |
|------|------|------|
| 플랫폼 | Flutter (iOS/Android) | 단일 코드베이스 |
| 영상 소스 | 앱 내 영상 (로컬 파일 + URL) | 시스템 오버레이/타 앱 캡처 제외 |
| 음성 인식 + 언어 감지 | Google Cloud Speech-to-Text | 자동 언어 감지 |
| 번역 | Google Cloud Translation API v2 (Basic) | API 키 인증 |
| 파이프라인 | 사전 처리(pre-process) | 실시간 스트리밍 아님(아래 3.2 참고) |

## 3. 동작 방식

### 3.1 파이프라인

```
영상 로드 → 오디오 추출(16kHz mono WAV) → STT(전사 + 단어 타임스탬프 + 언어감지)
        → 세그먼트 그룹화 → 번역(배치) → 자막 큐 생성 → 재생 위치 동기화 표시
```

각 단계는 서비스로 분리되어 있고, 핵심 로직(자막 동기화·그룹화·파싱)은 순수 Dart로
구현해 단위 테스트로 검증한다.

### 3.2 왜 "사전 처리" 방식인가

요구사항은 "실시간 번역 자막"이지만, **Flutter에서 재생 중인 영상의 내부 오디오 버퍼를
캡처할 안정적인 수단이 없다**(마이크 캡처만 가능). 따라서:

- **채택**: 영상 로드 직후 오디오를 한 번에 추출·전사·번역하여 타임스탬프가 박힌 자막 큐를
  만들고, 재생 시 위치에 맞춰 표시한다. 준비 단계(수 초)가 있지만 **재생 중 동기화는
  정확하고 드리프트가 없다**.
- **보류(Phase 2)**: 실시간 스트리밍 STT. 동일 `SpeechService` 인터페이스 뒤에 확장 가능하게
  설계만 열어 둠.

## 4. 기능 요구사항

### MVP (이번 범위)
- [x] 로컬 영상 파일 선택(`file_picker`) 및 URL 입력으로 재생
- [x] 오디오 추출(ffmpeg, 16kHz mono LINEAR16)
- [x] Google STT 동기 인식: 단어 타임스탬프 + 자동 언어 감지
- [x] 단어 → 자막 세그먼트 그룹화(간격/길이/시간/문장부호 기준)
- [x] Google Translate v2 배치 번역(BCP-47→ISO 매핑, source==target 생략)
- [x] 자막 큐 ↔ 재생 위치 동기화(이진탐색 + 캐시, 튜닝 오프셋)
- [x] 자막 오버레이 UI(원문 토글), 처리 진행 인디케이터
- [x] 설정: API 키 입력(secure storage), 대상 언어, 소스 언어 힌트
- [x] 핵심 로직 단위 테스트 + `flutter analyze` 클린

### Phase 2 (차기)
- [ ] 60초 초과 영상: GCS 업로드 + `longRunningRecognize` 폴링
- [ ] 실시간 스트리밍 인식
- [ ] 자막 SRT/VTT 내보내기·캐싱(재처리 생략)
- [ ] 백엔드 프록시(키 미노출), 서비스 계정/v3 번역
- [ ] 자막 편집·타이밍 보정 UI

## 5. 비기능 요구사항 / 제약

- **MVP 클립 길이**: 인라인 STT 60초 제한 → **≤60초 권장**(초과 시 명확한 오류).
- **네트워크 필수**: STT·번역은 클라우드 호출. 오프라인 미지원.
- **비용**: STT(분당)·번역(문자당) 과금. 세그먼트 배치로 호출 수 절감.
- **보안**: API 키는 커밋 금지. 자세한 규칙은 [`claude.md`](claude.md).
- **지원 언어(감지 후보)**: en, ko, ja, zh, es, fr, de (확장 가능, `AppConfig.languageCandidates`).

## 6. API 사용 개요

- **STT**: `POST speech.googleapis.com/v1/speech:recognize?key=…`
  (`LINEAR16`/16kHz/mono, `alternativeLanguageCodes`로 자동감지, `enableWordTimeOffsets`).
- **번역**: `POST translation.googleapis.com/language/translate/v2?key=…`
  (`q[]`, `target`, `source`, `format=text`).

엔드포인트·상수는 `lib/config/app_config.dart`에 집중.

## 7. 마일스톤

1. **M1 (완료)**: 프로젝트 골격 + 핵심 로직(모델·동기화 엔진·서비스·큐 빌더) + 단위 테스트.
2. **M2 (완료)**: UI(Home/Player/Settings) + 파이프라인 연결.
3. **M3**: 실기기 QA(추출·재생·실 API)로 E2E 타이밍 검증, 버그 기록.
4. **M4 (Phase 2)**: 장편 지원(GCS), 캐싱, 내보내기.

## 8. 검증 전략

- **자동(디바이스 불요)**: `flutter test`(동기화 엔진·모델·서비스·컨트롤러), `flutter analyze`.
- **수동(디바이스 필요)**: 실제 ffmpeg 추출, 영상 재생/렌더, 파일 선택, 실 Google API, 타이밍.

## 변경 이력

| 날짜 | 내용 |
|------|------|
| 2026-06-17 | 최초 작성. MVP 범위/파이프라인/API/마일스톤 정의. M1·M2 구현 완료(코드+테스트). |
